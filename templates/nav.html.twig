<nav class="navbar navbar-expand-lg navbar-light bg-warning" style="margin-bottom: 20px;">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{path('index')}}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('produits')}}">Produits</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('admin_categories')}}">Catégories</a>
            </li>


            {% if is_granted('ROLE_STAFF') %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Voir plus
                </a>

                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{path('ajoutProduct')}}">Ajout Produit</a>
                    <a class="dropdown-item" href="{{path('admin_ajoutCategorie')}}">Ajout Catégorie</a>
                    {% if is_granted('ROLE_ADMIN') %}<a class="dropdown-item" href="{{path('userList')}}">Manage
                        Users</a>{% endif %}
                </div>
            </li>
            {% endif %}
        </ul>
        <div>
            <div class="mb-3">
                
                {% if app.user %}
                
                {{ app.user.email }}
                <a style="color:black; margin-left: 15px;" href="#"><img style="height: 45px;
                    width: 36px;" src="https://icon-library.com/images/my-cart-icon/my-cart-icon-19.jpg" alt=""></a>
                <button class="btn btn-danger" style="margin-left: 15px;"><a style="color:black; text-decoration: none"
                        href="{{ path('app_logout') }}">Déconnexion</a> </button>
                {% else %}
                <button class="btn btn-primary"><a style="color:black; text-decoration: none"
                        href="{{ path('app_login') }}">Connexion</a></button>
                {% endif %}
            </div>
        </div>
    </div>
</nav>